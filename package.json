{
	"name": "eminence-astro-theme-cloudflare-pages",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && wrangler pages dev",
		"astro": "astro",
		"deploy": "astro build && wrangler pages deploy",
		"cf-typegen": "wrangler types",
		"format": "prettier --check ."
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.3.1",
		"astro": "^5.5.5"
	},
	"devDependencies": {
		"@types/node": "^22.13.14",
		"husky": "^9.1.7",
		"lint-staged": "^15.5.0",
		"prettier": "^3.5.3",
		"prettier-plugin-astro": "^0.14.1",
		"wrangler": "^4.6.0"
	},
	"lint-staged": {
		"*": "prettier --check"
	}
}
